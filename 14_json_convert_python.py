import json

x = {
  "name": "John",
  "age": 30,
  "city": "New York"
}

print(json.dumps(x, indent=4, separators==(","))

#to convert the files from python to json
#y= json.dumps(x)
#print(y)

# to convert the files from python to json
#z= json.loads(y)
#print(z["name"])
#print(z["age"])
#print(z["city"])